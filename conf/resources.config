/*
 * Resource configuration profiles for DADA2-NF
 * Define resource limits and scaling for different process types
 */

 // File intentionally left empty for:
 // 1) Sake of separating process logic and execution configuration
 // 2) Allowing process specific granularity